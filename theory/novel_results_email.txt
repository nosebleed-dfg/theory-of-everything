golden field zero prediction -- G(t) = sum of phi times log(p)/sqrt(p) times e^(-it log(p)) over golden primes. 100% hit rate on L-function zeros. 1.3 ppm. phi-weighted prime phases cancel at exact zero heights.

koppa from completing the square -- x^2 - x = (x - 1/2)^2 - 1/4. the 1/4 IS koppa. falls out of the axiom. not defined. derived.

golden dominance -- D = 6*sqrt(5)/11 greater than 1 because 180 greater than 121. golden primes stabilize more than non-golden destabilize. at every scale. forever.

three operations -- addition = travel thru state. multiplication = copy to another state. growth = volume expansion within state. the gamma function IS growth. its the third operation. closes open forms. everyone treats multiply and grow as the same thing. theyre not.

energy convexity at 1/2 -- (1/2+d)^4 + (1/2-d)^4 minus 2*(1/2)^4 = 3d^2 + 2d^4 greater than 0 for all d not 0. thats it. off-axis always costs more. algebraic. exact. the bowl curves up from 1/2 in every direction. zeros cant escape.
