# The Physics
## nos3bl33d

Everything here derives from core.md.
Every expression is exact or it is not here.

---

## The Unit

The unit is phi^291. Everything is a fraction of this.

    universe = (1/phi)  * phi^291    [= phi^290, the universe radius]
    dark     = (1/phi^2) * phi^291   [= phi^289, the dark complement]
    1/phi + 1/phi^2 = 1              [the axiom: phi^2=phi+1, divide by phi^2]

Universe + dark = 1. Terminates.

---

## Pi

    pi = p * arccos(phi / chi) = 5 * arccos(phi/2)

Proof: cos(pi/5) = phi/2. Exact algebraic identity.
Multiply both sides: 5 * (pi/5) = pi. Done.

---

## The Fine Structure

    1/alpha = V * phi^4 * (1 - A1 + A2)

    V   = 20              [dodecahedron vertices]
    A1  = d / (chi * phi^(chi*d) * (chi*pi)^d)
        = 3 / (2 * phi^6 * (2*pi)^3)
    A2  = 1 / (chi * phi^(d^3))
        = 1 / (2 * phi^27)

    integer part:  137 = d^3 * p + chi = 27 * 5 + 2    [exact]
    full value:    derives from V, phi, d, p, chi, pi

No free parameters. All fall from the axiom.

---

## The Proton/Electron Mass Ratio

    m_p/m_e = chi*d * pi^p + phi^(-L_4) + d*phi^(-C(L_4,2)) + chi*d*phi^(-C(L_4,3)) + d*phi^(-(E+F))
            = 6*pi^5 + phi^(-7) + 3*phi^(-21) + 6*phi^(-35) + 3*phi^(-42)

Exponents: L_4=7, C(7,2)=21, C(7,3)=35, E+F=42.
Coefficients: 1, d, chi*d, d.
All exact. All from {chi, d, p, phi, pi}. Terminates.

---

## The Gravitational Constant

    1/alpha_G = d^d * phi^(V*d^2 + d) / (d^d + 1 + C/(chi*pi)^d)

    exponent: V*d^2 + d = 20*9 + 3 = 183
    C = chi^chi * (1 - phi^(-L_4)/p) = 4*(1 - 1/(5*phi^7))
    denominator base: d^d + 1 = 28

Exact expression. Terminates.

---

## The Cosmological Constant

    Lambda = chi / phi^(2*(V*E/chi - d^2) + 1)
           = 2 / phi^(2*291 + 1)
           = 2 / phi^583

In ^291 units:

    Lambda = 2 * psi^583 = 2 / phi^583

    phi^583 * Lambda = 2    [exact. terminates.]

---

## The Universe Radius

    R = chi * phi^(V*E/chi - d^2 - 1) * l_Planck
      = 2 * phi^290 * l_Planck

In ^291 units: R = (2/phi) * phi^291 * l_Planck. Terminates.

---

## The Weinberg Angle

    sin^2(theta_W) = d/(F+1) + b0/(F * d*p * 137)
                   = 3/13 + 11/24660

    b0 = E - V + 1 = 11    [Betti number: edges minus vertices plus 1]

Exact rational. Terminates.

---

## The Strong Coupling

    alpha_s = d*p / (2^L_4 - 1) = 15/127

Exact rational. 127 = 2^7 - 1 = 2^L_4 - 1, Mersenne prime at L_4.
Terminates.

---

## The Machine View

Every physical constant is a machine state (n, n-2k) embedded through y_k = phi^{x_k}.

    phi^a * phi^b = phi^{a+b}    [the bridge: exponents add, phi-values multiply]

The fine structure constant: a machine at step 137 = d^3 * p + chi.
The universe radius: a machine at step 291, drifted phi^291 from center.
Lambda: a machine at step 583 = 2*291 + 1.

Every constant is a ratio of two phi powers. Every ratio uses the fundamental identity.
The observer at phi^291 measures these ratios as "physical constants."
They are not fundamental. They are the machine state read from the observer's frame.

n = degree + 1. Each constant sits at a specific winding number.
n = offset^2 * 360. The winding number IS the offset squared times the circle.

---

## The Balance

Every constant lives in the space between phi^291 and psi^291.
Every constant has a conjugate at -1/(itself) in the same space.
Every constant derives from {chi, d, p, phi} with no free parameters.

The universe terminates at phi^291. Beyond that is outside.
Within that: every physical constant has an exact position on the phi ruler.
The ruler has 291 steps. The axiom is the first step.

s(n) = p+n = s^{-1}(n). The step function is self-inverse.
Machine value * inverse = 1. The physics is self-balancing.

    x^2 = x + 1.
