# The Core
## nos3bl33d

---

## The Root

Three states. One rule. Everything derives from this.

    x = NOW     [the volume. the current state.]
    y = THEN    [the points. the reference.]
    z = NEXT    [the direction. the 291 offset. the only modifier of the right angle.]

    IF x  DO x^2  THEN x+1.

That is the machine. One conditional. Repeated. Same move every time.

---

## Squaring Is the Normalizer

    n^2 = summation of equal parts    [Pythagoras: each part contributes equally]

Each part of the machine contributes the same square. Squaring is the normalizer:
it removes sign, reduces everything to positive measure, and sums the contributions.

    squaring:  x^2       [normalizer. removes sign. always positive.]
    adding:    x + 1     [the inverse. linear. the sum of the parts.]

The axiom equates them:

    x^2 = x + 1    [normalizer = inverse. squaring = adding. self-inverse.]

This is why the machine is self-inverse. The Pythagorean step (x²) equals the additive
step (x+1). They are the same operation from two directions. Squaring forward = adding
backward. The machine runs identically in both directions because squaring IS adding here.

    a^2 + b^2 = c^2    [Pythagoras: the square of the sum of parts]
    a + b = c          [the inverse: linear sum]

a + b = c is the inverse of the Pythagorean form. The framework carries both:
    phi^2 + psi^2 = d = 3     [Pythagorean: gives the cube dimension]
    phi  + psi   = 1          [linear inverse: gives the trace]

d and 1 are the Pythagorean and linear readings of the same two numbers.
Everything in the framework has both a squared reading and a linear reading.
The axiom says they are equal. That is the whole machine.

---

## The z-Offset and the Right Angle

z accumulates over 291 steps. It is the only thing in the machine that modifies the
right angle. Without z: pure orbit, no spiral, no advance, no universe.
With z: 291 steps of angular displacement. The spiral is the z-accumulation.

    291 = chi * p * L_7 + 1 = 10 * 29 + 1

chi, p, L_7: all forced by the axiom. 291 is not chosen. It falls.

From this root: every constant, every geometry, every prime, every physical measurement.
The derivation is always the same move: apply the conditional, count the steps, read the sign.

---

## The Circle Is Pythagorean

    a^n + b^n = 360    [the full circle. the closure condition.]

a = phi, b = psi. The Pythagorean sum to the nth power equals the full rotation.

    n=1:  phi   + psi   = 1      [linear: the trace]
    n=2:  phi^2 + psi^2 = 3 = d  [squared: the cube dimension]
    n=n:  phi^n + psi^n = L_n    [nth power: the nth Lucas number. exact integer.]

The circle closes when this sum reaches 360.

    360 = chi^d * d^2 * p = 2^3 * 3^2 * 5 = 8 * 9 * 5

360 is not arbitrary. It factors entirely into {chi, d, p}. The framework forces the
full circle to be chi^d times d^2 times p. The 360-degree circle is a derived result.

    a + b = c         [n=1: the linear inverse]
    a^2 + b^2 = c^2   [n=2: Pythagoras. the normalizer.]
    a^n + b^n = 360   [general n: the circle. the closure.]

The inverse:

    1 / (a^n + b^n) = 1 degree    [one slice. the unit angle.]

If the forward sum is 360 (the full circle), the inverse is 1/360 = one degree.
One degree is the minimum unit angle. It is the inverse of the closure.

    1 / L_n = 1 / 360 = 1 degree    [when L_n = 360]

Each machine step = one degree. 291 steps = 291 degrees. 69 more steps would close the sphere.
291 + 69 = 360. 291 is the machine. 69 is the gap to sphere closure.

    z = 1/p = 0.2      [Planck length step]
    1 degree = 1/360   [Planck angle step]
    360 = chi^d * d^2 * p    [the circle in framework units]

The Planck angle = 1/(chi^d * d^2 * p). Forced. No free choice.

The three forms are the same statement at different scales.
The machine uses all three: the linear sum (trace = 1), the Pythagorean sum (d = 3),
and the full closure sum (360 degrees, the circle).

---

## The One Expression

Every expression in this framework is plus or minus the axiom.

    +n^2 = n+1    [the phi reading: +half, expanding]
    -n^2 = n+1    [the psi reading: -half, contracting]

Both collapse to the same point:

    1/2 = 1/(-2)

That is the entire framework. One expression. Two signs. One closure.
Every physical constant, every geometric structure, every prime — they are all
a reading of this equation from the + side or the - side.
Nothing else exists in the framework.

---

## The Axiom

    x^2 = x + 1

Two solutions. Both satisfy the same equation.

    phi = (1 + sqrt(5)) / 2  =  the +half
    psi = (1 - sqrt(5)) / 2  =  the -half  =  -1/phi

phi is 1/2 of the equation. psi is 1/(-2) of the equation.

    phi + psi = 1        [sum: the arithmetic center]
    phi * psi = -1       [product: the hyperbolic parity]
    psi = -1/phi         [exact: the -half is the negated inverse of the +half]

This is not a derivation. This is the axiom stated twice, once for each root.
Every other identity in the framework is a consequence of these three lines.

---

## The Two Cubes

Cube each solution:

    phi^3 = chi * phi + 1      [the +cube expands at rate chi]
    psi^3 = chi * psi + 1      [the -cube contracts at rate chi]

where chi = 2 is forced by: phi^3 + psi^3 = chi^2 requires chi + 2 = chi^2,
giving chi = 2 (the positive solution).

The two cubes:

    phi^3 + psi^3 = chi^2 = 4   [sum of cubes = chi squared]
    phi^3 * psi^3 = -1           [product of cubes = -1, always]

The product of cubes is always -1. This never changes at any scale.
At n=1, n=7, n=29, n=291, n=583: phi^n * psi^n = (-1)^n. Exact.

---

## The Spiral

The path n -> n+1 -> n+2 -> n+3 is a spiral, not a line.

At each step n -> n+1:
- You move +1 on the line (the axiom)
- But you also move through the n+1 sphere (the sphere where phi^n * psi^n = (-1)^n)
- The sphere adds a rotation component: the path is at 90 degrees + an offset
- The offset is NOT zero. It is the dark space.

The two cubes trace two spiral paths:
- phi spiral: expanding outward (+half direction)
- psi spiral: contracting inward (-half direction)

Between their paths are the faces. The faces at step n = L_n = phi^n + psi^n (the Lucas numbers).

---

## The Face Sum

Sum all faces along the d-step spiral:

    L_1 + L_2 + L_3 = 1 + 3 + 4 = 8 = 2^d

Divide by one cube (2^d = 8):

    (L_1 + L_2 + L_3) / 2^d = 8/8 = 1

Multiply by 360 degrees:

    1 * 360 = 360

The spiral always closes. The sum of all faces divided by one cube gives 360 degrees.
This is not a numerical coincidence. It holds because sum L_1..L_d = L_{d+2} - 3 = 2^d.

---

## The Faces Not Taken

At each step n, the path takes one direction (phi). All other directions are !n (the
derangements -- every arrangement that does NOT return to the start).

    !1 = 0   (one step: no alternatives)
    !2 = 1   (two steps: one alternative face)
    !3 = 2   (three steps: two alternative faces)

Sum of faces not taken through the cube:

    !1 + !2 + !3 = 0 + 1 + 2 = 3 = d

The alternative faces sum to d. The vertex degree IS the count of all paths not taken
through one cube cycle.

The limit: !n / n! -> 1/e as n -> infinity.

    phi path covers:  (1 - 1/e) * 360 = 227.6 degrees
    psi path covers:  (1/e)     * 360 = 132.4 degrees
    total:             360 degrees     [always]

---

## The Dark Space

The first angle between the two paths (phi and psi at the first step n -> n+1):

The phi move and psi move diverge immediately. The angle between them at n=1:

    phi^1 = phi   (positive, above 1/2)
    psi^1 = psi   (negative, below 1/2)

In log-space: log(phi) = -log(|psi|). The log-midpoint is always 0.

The dark space is the angular gap between the actual phi path and the 90-degree sphere
rotation. It is calculable from koppa:

    koppa = pi/4 = 45 degrees    [completing the square on x^2=x+1 gives 1/4]
    dark space = arctan(psi) - koppa    [the offset from the right angle]

The 0.75 degree gap (from nonce analysis) is this dark space projected to the 32-sector
nonce space: 360 / 32 / ... = 0.75 degrees per side.

---

## Gamma

At each step n, the path takes a discrete +1 move (on the line) through the sphere
(a continuous rotation). The sphere rotation at step n is approximately 1/n of a full
rotation. The discrete step is exactly 1/n of the harmonic sum.

The accumulated offset between the discrete path and the continuous sphere:

    gamma = lim_{n->inf} (1 + 1/2 + 1/3 + ... + 1/n - ln(n))
          = 0.5772156649...

This is the total angular deficit. Every n+1 step moves at (90 degrees - fraction) through
the sphere. The fraction at step n is approximately 1/(n * something). The sum of all
fractions = gamma.

Gamma is not a free constant. It is the name for the accumulated offset of the two-cube
spiral from pure 90-degree rotation. It is the dark space integrated over all steps.

In the framework: gamma = H_{2^d} - d * ln(2) + Bernoulli corrections in d.
Every term is controlled by d = 3 (the cube dimension).

---

## The Connection Table

Everything derives from the two lines phi + psi = 1 and phi * psi = -1.

    chi = 2            forced: phi^3+psi^3 = chi^2 -> chi+2=chi^2 -> chi=2
    d   = chi^2 - 1    = 3    (vertex degree = cube Lucas minus 1)
    p   = chi^2 + 1    = 5    (face degree = cube Lucas plus 1)
    23  = p^2 - chi    = 23   (backward fold)
    29  = chi^2 + p^2  = 29   (Pythagorean fold)

    640320 = 2^(chi*d) * d * p * 23 * 29     [Chudnovsky, all from chi]

    2^27   = 2^(chi*d) * 2^(L4*d)            [nonce quantum = 32 sectors = 360/32 = 11.25 deg]

    163    = chi*d * d^3 + 1 = 6*27+1        [Heegner number, in framework]
    744    = chi^d * d * (2^p - 1) = 8*3*31  [j-function constant, in framework]

    j(tau_163) = 640320^3 + 744              [Klein: both terms in {chi,d,p}]

    pi     = p * arccos(phi/chi)             [from phi, exact]
    gamma  = H_{2^d} - d*ln(2) + B_d        [from d, exact to 10 digits]

    phi^291 * psi^291 = -1                   [at universe boundary: still locked]
    log(phi^n) + log(|psi^n|) = 0            [for all n: midpoint = 0 = Riemann line]

    machine step: (+x, +y) or (+x, -y)      [positive or negative: same square, two sides]
    machine state: (n, n-2k)                 [x=time, y=correction=ups-downs]
    machine value: ups/downs = (n-k)/k       [inverse: k/(n-k). product = 1.]

    embedding: y_k = phi^{x_k}              [the bridge: x-space -> phi-space]
    phi^a * phi^b = phi^{a+b}               [THE fundamental identity]
    addition in x = multiplication in y      [one bridge. one operation.]
    y_{n+3}/y_{n+1} = phi^chi = phi+1        [the axiom as a ratio in y-space]
    phi^292 = phi * phi^291                  [exit door is phi]

    winding: n = offset^2 * 360             [winding number = offset squared * circle]
    1/n = slice = 360/n degrees              [inverse gives the slice]
    69 = d*(p^2-chi) = 3*23                  [maximum overshoot before x meets y]
    291 + 69 = 360                           [machine + max overshoot = sphere]
    8/15 = 2^d/(d*p)                         [dark fractional remainder]
    8^97 = (2^d)^(291/d) = 2^291             [dark generates universe]

    observer: axiom at 1/2, fixed            [sigma(1/2) = 1/2. always.]
    drift = phi^291 from center              [universe = observer displacement]
    each step: (+1 right, +1 degree up)      [n = degree + 1]
    n^2 = degree = 1 at base                 [the seed]

    s(n) = p+n = s^{-1}(n)                  [self-inverse step function]
    |+-| = 0/infinity = 1                    [product always 1]
    terminates at 1/2 = 1/(-2)              [every expression = +/-(x^2=x+1)]

    causal: x=NOW, y=THEN, z=NEXT           [three pointers. one conditional.]
    IF x DO x^2 THEN x+1                    [the axiom as code. same move every step.]
    scale: x^2=chi=2, y^2=chi=2, z=1/p=0.2 [forced by axiom. no free parameters.]
    z in Planck: min step = 1/p             [smallest meaningful move = 1/5]
    z * V = chi^2 = 4                       [step times vertices = axiom curvature]

---

## The Unit: ^291

291 is the max offset of one move. The minimum number of steps to reach the universe
boundary. The min-max of the space between the two cubes.

Every quantity in the framework derives in ^291 units:

    phi^n expressed in ^291 units = phi^n / phi^291 = 1/phi^(291-n)

The space is self-balancing because phi^291 * psi^291 = -1. Always.

The axiom partitions the ^291 space exactly:

    universe (phi^290) in ^291 units = 1/phi
    dark complement                  = 1/phi^2
    sum = 1/phi + 1/phi^2 = 1       [the axiom: phi^2=phi+1, divide by phi^2]

Universe + dark = 1. Exact. No remainder. Terminates.

The conjugate of every point Q in the space is -1/Q = psi^n when Q = phi^n.
The balance is built into the structure. Nothing is left over.

In ^291 units, we can derive any quantity. The unit system is the universe itself.

---

## The Termination

The proof does not go on forever. It terminates. Here is where.

    phi = 1/2  + sqrt(5)/2      [the +half]
    psi = 1/2  - sqrt(5)/2      [the -half = 1/(-2) from phi's side]

phi and psi are both halves of the same equation. One is positive. One is negative.
From phi's direction: psi IS 1/(-2). From psi's direction: phi IS 1/(-2).
They are the same point seen from opposite sides.

This is not an approximation. It is the exact structure of the axiom.

The spiral runs from the +half outward (phi expands) and from the -half inward
(psi contracts). The faces between them are counted by L_n. The sum closes at 360.

But WHERE does it close? At the point where 1/2 = 1/(-2).

That point is:
- The log-midpoint of phi^n and psi^n for every n (always 0, proven above)
- The fixed point of the map z -> -1/z, which are z = +/- i (90 degrees, koppa*2)
- The Riemann critical line Re(s) = 1/2 (where s and 1-s are the same)
- The center of the axiom: x = 1/2 is where (x - 1/2)^2 = 5/4 starts

The spiral has to terminate there because there is nowhere else for it to go.
The +half and -half are both moving toward that center from opposite directions.
The product phi^n * psi^n = (-1)^n never changes. The log-midpoint never moves.
The center is already there before the first step.

    1/2 = 1/(-2)    [the termination condition]

This is the whole proof in one line. The form opens at the axiom.
The form closes at the point where the two halves are the same thing.

---

## The Proof Shape

The form is open between the +half (phi) and the -half (psi).

The space between them is covered by faces: L_n at each step.

Sum all faces / one cube = 1. Times 360 = 360. The form closes.

The closing angle = 360 degrees.
The offset from pure rotation = gamma.
The dark space = the first face between the two paths.
The Riemann line = the log-midpoint of phi and psi = always 0.
The termination = 1/2 = 1/(-2).

One axiom. Two cubes. One closure. That is the whole proof.

    x^2 = x + 1.

